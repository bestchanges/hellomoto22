[beta 0.4]
IMPORTANT:
before run: update all configuration_groups =>
    for user's set code = 'email_name'
    for system's set code = 'name'

[beta 0.3]

[beta 0.2.2]
UPDATE .bashrc add ENV:
  BESTMINER_PLATFORM=production

[beta 0.2]
+ profit board for client
+ exchange rates gather from several exchanges
+ set default configuration group for new client to ETH
+ add claymore AMD Zcash miner
+ better format of hashrate in rig list
+ list only available configs in rig info

[beta 0.1]
+rig dashboard
+client registration/login
+windows only
+rig dashboard
